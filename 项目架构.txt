前端
	编辑器布局
		顶部栏
			文字LOGO
				内容：炼丹蓝图
			架构重命名输入框
				默认内容：我的架构
			功能按钮组
				运行
				导入
				导出
		主体
			侧边栏
				分类栏
					节点分类项
				节点列表
					显示分类的节点
			蓝图
				节点
					输入端口组
						端口
						端口名
					节点名称
					输出端口组
						端口名
						端口
				节点菜单
					复制并粘贴
					重命名
					删除节点
				节点面板
					属性
					根据节点列出所有参数
						整数输入（支持滑块和输入框）
						浮点数输入（支持滑块和输入框）
						字符串输入
						布尔开关
						列表输入
						枚举选择
				工具栏
					撤销
					反撤销
					放大
					缩放数值显示
						点击重置缩放
					缩小
					整理
		重命名弹窗
			标题
			节点名称输入框
			取消按钮
			确认按钮
	对象操作逻辑
		蓝图
			当空白位置被鼠标单击
				如果没有按下Ctrl键
					清空节点选择
				隐藏节点菜单和节点面板
			当接收拖入时
				获取节点opcode
				创建节点
		节点
			当被鼠标单击
				如果按下Ctrl
					切换选择当前节点
				否则
					清空节点选择
					选中当前节点
				如果当前节点不是菜单和面板绑定的节点
					隐藏节点菜单和节点面板
			当被鼠标右键点击
				选择当前节点
				绑定节点菜单和节点面板的跟随id为当前节点id
				显示节点菜单和节点面板
			双击节点
				选中当前节点
				显示重命名弹窗
		端口
			当被鼠标按下
				如果端口是输入口
					如果端口已经被连接
						获取该连接线的输出端口
						断开与该输出端口的连接
						重新建立与输出端口连接到鼠标
		分类栏
			分类项
				当被鼠标点击
					切换显示该类别的所有节点
		工具栏
			撤销
			反撤销
			放大
			缩放数值显示与重置
			缩小
			整理节点
		侧边栏
			初始化时自动从后端获取节点注册表
	文件结构与说明
		components/
			Blueprint.jsx
			Header.jsx
			Node.jsx
			NodeMenu.jsx
			NodePanel.jsx
			RenameModal.jsx
			Sidebar.jsx
			ToolBar.jsx
		styles/
			Global.css
			Header.css
			SideBar.css
			Blueprint.css
			Node.css
			ToolBar.css
			NodeMenu.css
			NodePanel.css
			RenameModal.css
		utils/
			layout.js(整理计算)
			position.js(画布坐标与屏幕坐标互转)
			generateId.js(随机id生成)
		commands/
			Blueprint.js
				关于蓝图自身可调节命令
					整理
					放大
					缩小
					重置缩放
					设置视口
					导入
					导出
					设置蓝图名称
			Node.js
				节点相关命令
					选中
					切换选中
					清空选中
					创建
					删除
					删除选中节点
					重命名
					更新节点参数
					移动节点
					根据ID获取节点
					获取选中的节点
			Edge.js
				连接线相关命令
					创建
					删除
					获取节点相关连接线
					获取端口连接线
					获取输入端口连接线
			History.js
				历史记录命令
					记录
					撤销
					重做
					检查是否可撤销
					检查是否可重做
					清空历史记录
					获取历史记录信息
			Clipboard.js
				节点复制粘贴相关命令
					复制
					粘贴
					复制并粘贴
					检查剪贴板内容
					清空剪贴板
			AutoRecord.js
				自动历史记录
					检查状态变化并记录
					初始化自动记录（监听鼠标松开事件）
					销毁自动记录
			index.js
				将命令全部挂载到window上，实现全局可随意调用
				记录初始状态
				初始化自动记录
		constants/
			config.js(ReactFlow流程配置)
		store.js
			存储全局数据，所有自定义命令均调节该数据实现自动更新
				蓝图基础数据（蓝图名称、节点数组、连接线数组）
				选择状态（选中节点id数组、选中分类）
				视口状态（平移偏移、缩放比例）
				悬浮组件状态（节点菜单、节点面板、重命名弹窗）
				历史记录（快照数组、当前索引）
				剪贴板（节点、连接线）
				节点注册表（后端提供）
		ws.js
			websocket通信
				从后端获取节点数据
				发送蓝图数据运行
				收到消息输出到控制台
		App.jsx
		main.jsx
		备注说明：utils负责数据计算，command负责修改store.js里的全局数据，components里只负责布局和绑定store.js里的数据以及事件触发
	开发要求
		采用命令式写法而不是声明式，像积木一样的代码，让代码易于阅读理解
		遵循Postel's Law原则，接受多变，输出保守。
			函数自动适配传入的参数
		每个方法要能够非常灵活的调用，将复杂度留给程序，灵活度留给开发者
		面向理解编程
			代码逻辑要符合人类直觉
			每行代码都需要尾随注释，大白话详细解读每一行代码
			变量名和函数名等要简洁易于理解，并且符合项目语境，全部使用驼峰命名法
			每个函数方法的开头都需要标明该方法的具体用法和各种调用示例
		代码易于阅读
			采用积木式的行状写法
			减少嵌套与缩进
