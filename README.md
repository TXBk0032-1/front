# 炼丹蓝图 - 项目开发文档

## 📋 项目概述

**炼丹蓝图**是一个基于React和ReactFlow的可视化节点编辑器，用于构建和运行AI模型的蓝图架构。项目采用命令式编程风格，代码结构清晰，易于理解和维护。

### 核心特性

- 🎨 **可视化节点编辑** - 拖拽创建节点，连线构建数据流
- 🔄 **撤销/重做** - 完整的历史记录系统
- 📋 **复制粘贴** - 支持节点的复制和粘贴操作
- 🔍 **缩放平移** - 自由缩放和平移画布视图
- 🎯 **节点参数编辑** - 右键节点可编辑参数
- 🌐 **WebSocket通信** - 与后端实时通信，运行蓝图
- 💾 **导入导出** - 保存和加载蓝图JSON文件

### 技术栈

- **React 19** - UI框架
- **ReactFlow 12** - 节点编辑器核心库
- **Zustand 5** - 轻量级状态管理
- **HeroUI 3** - UI组件库
- **Vite 7** - 构建工具
- **TailwindCSS 4** - 样式框架

---

## 📁 项目结构

```
front/
├── src/
│   ├── main.jsx              # 应用入口，初始化React和全局命令
│   ├── App.jsx               # 根组件，组织整体布局
│   ├── store.js              # Zustand全局状态管理
│   ├── ws.js                 # WebSocket通信管理器
│   │
│   ├── commands/             # 命令系统（核心业务逻辑）
│   │   ├── index.js          # 命令挂载到window.cmd
│   │   ├── Node.js           # 节点相关命令
│   │   ├── Edge.js           # 连接线相关命令
│   │   ├── Blueprint.js      # 蓝图相关命令
│   │   ├── History.js        # 历史记录命令
│   │   ├── Clipboard.js      # 剪贴板命令
│   │   └── AutoRecord.js     # 自动历史记录
│   │
│   ├── components/           # React组件（UI布局）
│   │   ├── Header.jsx        # 顶部栏
│   │   ├── Sidebar.jsx       # 侧边栏（分类和节点列表）
│   │   ├── Blueprint.jsx     # 蓝图画布
│   │   ├── Node.jsx          # 节点组件
│   │   ├── ToolBar.jsx       # 工具栏
│   │   ├── NodeMenu.jsx      # 节点右键菜单
│   │   ├── NodePanel.jsx     # 节点属性面板
│   │   └── RenameModal.jsx   # 重命名弹窗
│   │
│   ├── utils/                # 工具函数（复杂计算）
│   │   ├── generateId.js     # ID生成器
│   │   ├── position.js       # 坐标转换工具
│   │   └── layout.js         # 节点布局算法
│   │
│   ├── constants/            # 常量配置
│   │   ├── config.js         # ReactFlow配置
│   │   └── registry_example.json  # 节点注册表示例
│   │
│   ├── styles/               # CSS样式文件
│   │   ├── Global.css        # 全局样式
│   │   ├── Header.css        # 顶部栏样式
│   │   ├── SideBar.css       # 侧边栏样式
│   │   ├── Blueprint.css     # 蓝图样式
│   │   ├── Node.css          # 节点样式
│   │   ├── ToolBar.css       # 工具栏样式
│   │   ├── NodeMenu.css      # 节点菜单样式
│   │   ├── NodePanel.css     # 节点面板样式
│   │   └── RenameModal.css   # 重命名弹窗样式
│   │
│   └── assets/               # 静态资源（图标、图片）
│
├── public/                   # 公共资源
├── index.html                # HTML入口
├── package.json              # 项目依赖配置
├── vite.config.js            # Vite配置
└── 开发目标.txt              # 项目需求文档
```

## 🏗️ 架构设计

### 设计理念

项目遵循以下核心设计原则（来自[`开发目标.txt`](开发目标.txt:149)）：

1. **命令式写法** - 采用命令式而非声明式，代码像积木一样组合，易于阅读理解
2. **Postel's Law原则** - 接受多变，输出保守。函数自动适配传入的参数
3. **灵活调用** - 每个方法能够非常灵活地调用，将复杂度留给程序，灵活度留给开发者
4. **面向理解编程** - 代码逻辑符合人类直觉，每行代码都有尾随注释详细解读
5. **易于阅读** - 采用积木式的行状写法，减少嵌套与缩进

### 三层架构

项目采用清晰的三层架构（参考[`开发目标.txt`](开发目标.txt:148)）：

```
┌─────────────────────────────────────────────────────────┐
│  Components（组件层）                                     │
│  - 负责UI布局和渲染                                       │
│  - 绑定store数据实现自动更新                              │
│  - 触发事件调用commands                                   │
└─────────────────────────────────────────────────────────┘
                          ↓ 调用
┌─────────────────────────────────────────────────────────┐
│  Commands（命令层）                                       │
│  - 封装所有业务逻辑                                       │
│  - 修改store中的全局数据                                  │
│  - 调用utils进行复杂计算                                  │
│  - 挂载到window.cmd，全局可调用                           │
└─────────────────────────────────────────────────────────┘
                          ↓ 调用
┌─────────────────────────────────────────────────────────┐
│  Utils（工具层）                                          │
│  - 纯函数，负责复杂计算                                   │
│  - 不修改全局状态                                         │
│  - 可独立测试                                             │
└─────────────────────────────────────────────────────────┘
                          ↓ 读写
┌─────────────────────────────────────────────────────────┐
│  Store（状态层）                                          │
│  - Zustand管理全局状态                                    │
│  - 所有数据的唯一来源                                     │
│  - 状态变化自动触发组件更新                               │
└─────────────────────────────────────────────────────────┘
```

### 数据流

```
用户操作 → 组件事件 → 调用命令 → 修改Store → 组件自动更新
```

**示例：创建节点的完整流程**

1. 用户从侧边栏拖拽节点到画布
2. [`Blueprint.jsx`](src/components/Blueprint.jsx:213) 的 `handleDrop` 事件触发
3. 调用 [`window.cmd.createNode()`](src/commands/Node.js:94)
4. 命令函数修改 [`store.nodes`](src/store.js:29)
5. 所有订阅 `nodes` 的组件自动重新渲染

---

## 📦 核心模块详解

### 1. Store（状态管理）

**文件：** [`src/store.js`](src/store.js:1)

使用Zustand管理全局状态，所有数据的唯一来源。

#### 状态结构

```javascript
{
  // 蓝图基础数据
  blueprintName: "我的架构",        // 蓝图名称
  nodes: [],                        // 节点数组
  edges: [],                        // 连接线数组
  
  // 选择状态
  selectedIds: [],                  // 选中的节点ID数组
  selectedCategory: "all",          // 当前选中的分类
  
  // 视口状态
  viewport: { x: 0, y: 0, zoom: 1 }, // 视口平移和缩放
  
  // 悬浮组件状态
  nodeMenu: { visible: false, nodeId: null },      // 节点菜单
  nodePanel: { visible: false, nodeId: null },     // 节点面板
  renameModal: { visible: false, nodeIds: [], ... }, // 重命名弹窗
  
  // 历史记录
  history: [],                      // 历史快照数组
  historyIndex: -1,                 // 当前历史索引
  
  // 剪贴板
  clipboard: { nodes: [], edges: [] }, // 复制的节点和连接线
  
  // 节点注册表（后端提供）
  registry: { categories: {}, nodes: {} } // 所有可用节点类型
}
```

#### 使用方式

```javascript
// 在组件中使用hook获取状态
import { useStore } from './store'
const nodes = useStore(s => s.nodes)

// 在任意位置直接获取状态
import { getState } from './store'
const currentNodes = getState().nodes

// 在任意位置直接修改状态
import { setState } from './store'
setState({ nodes: [...] })
```

---

### 2. Commands（命令系统）

**目录：** [`src/commands/`](src/commands/)

命令系统是项目的核心，封装所有业务逻辑。所有命令都挂载到 `window.cmd` 上，可在任意位置调用。

#### 命令分类

| 文件                                            | 职责       | 主要命令                                                                  |
| ----------------------------------------------- | ---------- | ------------------------------------------------------------------------- |
| [`Node.js`](src/commands/Node.js:1)             | 节点操作   | `createNode`, `deleteNode`, `renameNode`, `selectNode`, `updateNodeParam` |
| [`Edge.js`](src/commands/Edge.js:1)             | 连接线操作 | `createEdge`, `deleteEdge`, `getEdgesByNode`                              |
| [`Blueprint.js`](src/commands/Blueprint.js:1)   | 蓝图操作   | `arrange`, `zoomIn`, `zoomOut`, `importBlueprint`, `exportBlueprint`      |
| [`History.js`](src/commands/History.js:1)       | 历史记录   | `record`, `undo`, `redo`, `canUndo`, `canRedo`                            |
| [`Clipboard.js`](src/commands/Clipboard.js:1)   | 剪贴板     | `copy`, `paste`, `copyAndPaste`                                           |
| [`AutoRecord.js`](src/commands/AutoRecord.js:1) | 自动记录   | `initAutoRecord`, `checkAndRecord`                                        |

#### 命令调用示例

```javascript
// 创建节点
window.cmd.createNode('Conv2D', { x: 100, y: 100 })

// 删除选中的节点
window.cmd.deleteSelectedNodes()

// 撤销操作
window.cmd.undo()

// 复制并粘贴
window.cmd.copyAndPaste()

// 整理布局
window.cmd.arrange()
```

#### 命令设计原则

1. **参数灵活** - 支持多种参数格式，自动适配
2. **返回值明确** - 成功返回结果对象，失败返回null或false
3. **注释详细** - 每个函数都有详细的用法说明和示例
4. **纯业务逻辑** - 不包含UI代码，只操作数据

### 3. Components（组件系统）

**目录：** [`src/components/`](src/components/)

组件只负责UI布局和渲染，不包含业务逻辑。所有数据从store获取，所有操作通过调用commands完成。

#### 组件结构

```
App.jsx                    # 根组件
├── Header.jsx             # 顶部栏
│   ├── Logo               # 文字LOGO "炼丹蓝图"
│   ├── NameInput          # 架构名称输入框
│   └── ActionButtons      # 功能按钮（运行、导入、导出）
│
├── Workspace              # 主工作区
│   ├── Sidebar.jsx        # 侧边栏
│   │   ├── CategoryBar    # 分类栏
│   │   └── NodeList       # 节点列表
│   │
│   └── Blueprint.jsx      # 蓝图画布
│       ├── Node.jsx       # 节点组件
│       ├── ToolBar.jsx    # 工具栏
│       ├── NodeMenu.jsx   # 节点右键菜单
│       └── NodePanel.jsx  # 节点属性面板
│
└── RenameModal.jsx        # 重命名弹窗
```

#### 关键组件说明

**[`Header.jsx`](src/components/Header.jsx:1)** - 顶部栏
- 显示Logo和蓝图名称
- 提供运行、导入、导出功能
- 运行按钮调用 [`ws.runBlueprint()`](src/ws.js:245)

**[`Sidebar.jsx`](src/components/Sidebar.jsx:1)** - 侧边栏
- 显示节点分类和节点列表
- 支持拖拽节点到画布创建
- 从 [`store.registry`](src/store.js:57) 读取节点数据

**[`Blueprint.jsx`](src/components/Blueprint.jsx:1)** - 蓝图画布
- ReactFlow容器，管理节点和连接线
- 处理拖放创建节点
- 处理空白区域点击清空选择

**[`Node.jsx`](src/components/Node.jsx:1)** - 节点组件
- 渲染节点的输入端口、名称、输出端口
- 处理点击、右键、双击事件
- 支持拖拽已连接的输入端口断线重连

**[`ToolBar.jsx`](src/components/ToolBar.jsx:1)** - 工具栏
- 撤销/重做按钮
- 缩放控制（放大、缩小、重置）
- 整理布局按钮

**[`NodeMenu.jsx`](src/components/NodeMenu.jsx:1)** - 节点右键菜单
- 复制并粘贴
- 重命名
- 删除节点

**[`NodePanel.jsx`](src/components/NodePanel.jsx:1)** - 节点属性面板
- 显示节点的所有参数
- 支持多种参数类型：int、float、bool、str、list、enum
- 实时修改参数值

**[`RenameModal.jsx`](src/components/RenameModal.jsx:1)** - 重命名弹窗
- 双击节点或点击菜单重命名时显示
- 支持回车确认、ESC取消

---

### 4. Utils（工具函数）

**目录：** [`src/utils/`](src/utils/)

工具函数是纯函数，只负责复杂计算，不修改全局状态。

#### [`generateId.js`](src/utils/generateId.js:1) - ID生成器

生成唯一的随机ID，用于节点和连接线的标识。

```javascript
import { generateNodeId, generateEdgeId } from './utils/generateId'

const nodeId = generateNodeId()    // "node_abc123..."
const edgeId = generateEdgeId()    // "edge_def456..."
```

**实现原理：**
- 使用 `performance.now()` 获取高精度时间戳
- 使用 `crypto.getRandomValues()` 生成加密安全的随机数
- 组合时间戳和随机数确保唯一性

#### [`position.js`](src/utils/position.js:1) - 坐标转换工具

实现画布坐标与屏幕坐标的相互转换，考虑视口的平移和缩放。

```javascript
import { screenToCanvas, canvasToScreen } from './utils/position'

// 屏幕坐标转画布坐标（鼠标点击位置 → 画布位置）
const canvasPos = screenToCanvas(mouseX, mouseY)

// 画布坐标转屏幕坐标（节点位置 → 屏幕显示位置）
const screenPos = canvasToScreen(nodeX, nodeY)
```

**转换公式：**
```javascript
// 屏幕 → 画布
canvasX = (screenX - offsetX) / zoom
canvasY = (screenY - offsetY) / zoom

// 画布 → 屏幕
screenX = canvasX * zoom + offsetX
screenY = canvasY * zoom + offsetY
```

#### [`layout.js`](src/utils/layout.js:1) - 节点布局算法

自动整理节点布局，使用简单的层级布局算法。

```javascript
import { arrangeNodes } from './utils/layout'

// 整理所有节点
const newPositions = arrangeNodes(nodes, edges)

// 自定义间距
const newPositions = arrangeNodes(nodes, edges, {
  gapX: 300,  // 水平间距
  gapY: 150   // 垂直间距
})
```

**算法原理：**
1. 使用BFS（广度优先搜索）计算节点层级
2. 入度为0的节点作为第一层
3. 按层级和索引计算节点位置
4. 孤立节点放在最后一层

---

### 5. WebSocket通信

**文件：** [`src/ws.js`](src/ws.js:1)

封装WebSocket通信逻辑，与后端实时通信。

#### 主要功能

**连接管理**
```javascript
import ws from './ws'

// 连接服务器
await ws.connect()

// 检查连接状态
if (ws.isConnected()) { ... }

// 断开连接
ws.disconnect()
```

**获取节点注册表**
```javascript
// 从后端获取所有可用节点类型
await ws.getRegistry()
// 数据会自动更新到 store.registry
```

**运行蓝图**
```javascript
// 运行当前蓝图
await ws.runBlueprint()

// 运行指定蓝图
await ws.runBlueprint(customBlueprint)
```

#### 消息格式

**发送消息：**
```javascript
{
  id: "msg-1-1234567890",      // 消息ID
  type: "getRegistry",          // 消息类型
  data: { ... }                 // 消息数据
}
```

**接收消息：**
```javascript
{
  id: "msg-1-1234567890",      // 对应的请求ID
  type: "getRegistry",          // 消息类型
  data: { ... }                 // 响应数据
}
```

#### 节点注册表格式

参考 [`registry_example.json`](src/constants/registry_example.json:1)

```json
{
  "categories": {
    "basic": {
      "label": "基础",
      "color": "#8B92E5",
      "nodes": ["input", "output"]
    }
  },
  "nodes": {
    "input": {
      "opcode": "input",
      "label": "输入",
      "ports": {
        "input": {},
        "output": { "out": "输出" }
      },
      "params": {
        "out_shape": {
          "label": "输出维度",
          "type": "list",
          "value": [2, 4, 10]
        }
      }
    }
  }
}
```
## 💻 开发指南

### 如何添加新节点类型

新节点类型由后端定义并通过WebSocket发送，前端自动渲染。

**后端需要提供的节点定义：**

```json
{
  "opcode": "my_node",           // 节点唯一标识
  "label": "我的节点",            // 显示名称
  "category": "basic",           // 所属分类
  "ports": {
    "input": {                   // 输入端口（对象格式）
      "x": "输入X",              // key是端口ID，value是显示名称
      "y": "输入Y"
    },
    "output": {                  // 输出端口（对象格式）
      "result": "结果"
    }
  },
  "params": {                    // 参数定义
    "threshold": {
      "label": "阈值",
      "type": "float",           // 类型：int/float/bool/str/list/enum
      "range": [0, 1],           // 可选：范围限制
      "value": 0.5               // 默认值
    }
  }
}
```

前端会自动：
1. 在侧边栏显示该节点
2. 支持拖拽创建
3. 渲染输入输出端口
4. 生成参数编辑界面

### 如何添加新命令

在 [`src/commands/`](src/commands/) 目录下创建或修改命令文件。

**示例：添加一个新命令**

```javascript
// src/commands/MyCommands.js

import { getState, setState } from '../store'

/**
 * myCommand - 我的命令
 *
 * 用法示例：
 *   myCommand()                    // 基本用法
 *   myCommand({ option: true })    // 带选项
 *
 * @param {Object} options - 配置选项
 * @returns {boolean} - 是否成功
 */
export function myCommand(options = {}) {
  const { nodes } = getState()                    // 获取当前状态
  
  // 执行业务逻辑
  const newNodes = nodes.map(node => {
    // 处理节点...
    return node
  })
  
  setState({ nodes: newNodes })                   // 更新状态
  
  return true                                     // 返回结果
}
```

**在 [`index.js`](src/commands/index.js:1) 中导出：**

```javascript
import { myCommand } from './MyCommands'

const cmd = {
  // ... 其他命令
  myCommand,
}

window.cmd = cmd

export { myCommand }
```

### 如何添加新组件

在 [`src/components/`](src/components/) 目录下创建新组件。

**组件开发规范：**

1. **只负责UI** - 不包含业务逻辑
2. **从store获取数据** - 使用 `useStore` hook
3. **调用commands操作** - 通过 `window.cmd.xxx()`
4. **详细注释** - 每行代码都有尾随注释

**示例：创建一个新组件**

```javascript
// src/components/MyComponent.jsx

import { useStore } from '../store'              // 导入store hook
import '../styles/MyComponent.css'               // 导入样式

/**
 * MyComponent - 我的组件
 *
 * 用法示例：
 *   <MyComponent />
 *
 * 组件职责：
 *   显示某些信息并处理用户交互
 */
function MyComponent() {
  const data = useStore(s => s.someData)         // 从store获取数据
  
  const handleClick = () => {                    // 点击事件处理
    window.cmd.someCommand()                     // 调用命令
  }
  
  return (                                       // 返回JSX结构
    <div className="my-component">
      <button onClick={handleClick}>
        {data}
      </button>
    </div>
  )
}

export default MyComponent                       // 导出组件
```

### 如何修改样式

样式文件位于 [`src/styles/`](src/styles/) 目录。

**注意事项：**
- 根据[`开发目标.txt`](开发目标.txt:92)，样式文件已完成，**禁止乱动**
- 如需修改，请先理解现有样式结构
- 使用CSS变量实现主题色和动态样式
- 保持样式的模块化和可维护性

---

## 📝 代码规范

### 命名规范

遵循项目的命名约定（参考[`开发目标.txt`](开发目标.txt:157)）：

- **变量和函数** - 驼峰命名法 `camelCase`
- **组件** - 大驼峰命名法 `PascalCase`
- **常量** - 全大写下划线 `UPPER_SNAKE_CASE`
- **文件名** - 与导出内容一致

**示例：**
```javascript
const nodeCount = 10                    // 变量：驼峰
function createNode() { }               // 函数：驼峰
const MyComponent = () => { }           // 组件：大驼峰
const MAX_HISTORY = 50                  // 常量：全大写
```

### 注释规范

每个函数都需要详细的注释（参考[`开发目标.txt`](开发目标.txt:158)）：

```javascript
/**
 * functionName - 函数简短描述
 *
 * 用法示例：
 *   functionName(arg1, arg2)                    // 基本用法
 *   functionName({ option: true })              // 带选项
 *
 * @param {Type} paramName - 参数说明
 * @returns {Type} - 返回值说明
 */
export function functionName(paramName) {
  const result = doSomething()                   // 每行代码都有尾随注释，大白话解释
  return result                                  // 返回结果
}
```

### 代码风格

遵循以下代码风格（参考[`开发目标.txt`](开发目标.txt:149)）：

1. **命令式写法** - 像积木一样的代码，易于阅读
2. **减少嵌套** - 尽量扁平化，避免深层嵌套
3. **行状写法** - 每行一个操作，清晰明了
4. **符合直觉** - 代码逻辑符合人类思维

**好的示例：**
```javascript
// ✅ 好的写法：扁平、清晰
function processNode(nodeId) {
  const node = getNodeById(nodeId)               // 获取节点
  if (!node) return null                         // 提前返回，减少嵌套
  
  const newData = transformData(node.data)       // 转换数据
  const result = saveNode(node.id, newData)      // 保存节点
  return result                                  // 返回结果
}

// ❌ 不好的写法：嵌套深、难读
function processNode(nodeId) {
  const node = getNodeById(nodeId)
  if (node) {
    const newData = transformData(node.data)
    if (newData) {
      const result = saveNode(node.id, newData)
      if (result) {
        return result
      }
    }
  }
  return null
}
```

### 参数灵活性

函数应该支持多种参数格式（参考[`开发目标.txt`](开发目标.txt:151)）：

```javascript
/**
 * createEdge - 创建连接线（支持多种参数格式）
 */
export function createEdge(configOrFromNodeId, fromPortName, toNodeId, toPortName) {
  let from, to
  
  if (typeof configOrFromNodeId === 'object') {  // 对象格式
    from = configOrFromNodeId.from
    to = configOrFromNodeId.to
  } else if (typeof configOrFromNodeId === 'string') { // 字符串格式
    from = { nodeId: configOrFromNodeId, portName: fromPortName }
    to = { nodeId: toNodeId, portName: toPortName }
  }
  
  // 创建连接线...
}

// 两种调用方式都支持：
createEdge({ from: {...}, to: {...} })           // 对象格式
createEdge('node1', 'out', 'node2', 'in')        // 字符串格式
```

## 🐛 故障排查

### 常见问题

**1. WebSocket连接失败**

**现象：** 控制台显示 "WebSocket连接失败"

**原因：** 后端服务未启动或地址不正确

**解决：**
- 确保后端服务已启动
- 检查 [`ws.js`](src/ws.js:25) 中的 `WS_URL` 是否正确
- 默认地址：`ws://localhost:8765`

**2. 节点列表为空**

**现象：** 侧边栏没有显示任何节点

**原因：** 未获取节点注册表或注册表格式错误

**解决：**
- 点击顶部栏的"运行"按钮前会自动获取节点
- 手动调用：`window.cmd` 不存在，应该是 `ws.getRegistry()`
- 检查后端返回的注册表格式是否符合规范

**3. 节点无法连接**

**现象：** 拖拽端口无法创建连接线

**原因：** 端口类型不匹配或ReactFlow配置问题

**解决：**
- 确保从输出端口拖到输入端口
- 检查 [`config.js`](src/constants/config.js:1) 中的ReactFlow配置
- 查看控制台是否有错误信息

**4. 撤销/重做不工作**

**现象：** 点击撤销按钮没有反应

**原因：** 历史记录未正确初始化或记录

**解决：**
- 检查 [`AutoRecord.js`](src/commands/AutoRecord.js:1) 是否正确初始化
- 查看 `store.history` 是否有记录
- 确保操作后触发了 `record()` 函数

**5. 节点参数修改不生效**

**现象：** 在属性面板修改参数后没有保存

**原因：** 参数更新逻辑错误

**解决：**
- 检查 [`NodePanel.jsx`](src/components/NodePanel.jsx:389) 中的 `updateNodeParam` 函数
- 确保参数格式正确
- 查看控制台是否有错误

### 调试技巧

**1. 查看全局状态**

在浏览器控制台中：
```javascript
// 查看当前状态
window.store = require('./store').getState
window.store()

// 或直接访问
window.cmd
```

**2. 查看历史记录**

```javascript
// 查看历史信息
window.cmd.getHistoryInfo()

// 查看完整历史
window.cmd.getState().history
```

**3. 手动测试命令**

```javascript
// 创建节点
window.cmd.createNode('input', { x: 100, y: 100 })

// 删除节点
window.cmd.deleteNode('node_xxx')

// 撤销
window.cmd.undo()
```

**4. 查看节点注册表**

```javascript
// 查看注册表
window.cmd.getState().registry
```

---

## 📚 学习路径

### 新手入门（1-2天）

1. **了解项目结构** - 阅读本文档的"项目结构"部分
2. **运行项目** - 按照"快速开始"启动项目
3. **理解架构** - 学习"架构设计"中的三层架构
4. **查看示例** - 运行项目，体验各种交互

### 深入理解（3-5天）

1. **学习Store** - 阅读 [`store.js`](src/store.js:1)，理解状态管理
2. **学习Commands** - 阅读 [`commands/`](src/commands/) 目录，理解命令系统
3. **学习Components** - 阅读 [`components/`](src/components/) 目录，理解组件结构
4. **学习Utils** - 阅读 [`utils/`](src/utils/) 目录，理解工具函数

### 开始开发（5天+）

1. **添加简单功能** - 尝试添加一个新的工具栏按钮
2. **修改现有功能** - 修改节点样式或交互逻辑
3. **添加复杂功能** - 添加新的命令或组件
4. **优化性能** - 优化渲染性能或算法效率

### 推荐阅读顺序

1. [`开发目标.txt`](开发目标.txt:1) - 了解项目需求和设计理念
2. [`main.jsx`](src/main.jsx:1) - 了解应用入口和初始化流程
3. [`store.js`](src/store.js:1) - 了解全局状态结构
4. [`commands/index.js`](src/commands/index.js:1) - 了解所有可用命令
5. [`App.jsx`](src/App.jsx:1) - 了解整体布局结构
6. [`Blueprint.jsx`](src/components/Blueprint.jsx:1) - 了解核心画布逻辑
7. [`Node.jsx`](src/components/Node.jsx:1) - 了解节点渲染和交互

---

## 🎓 核心概念总结

### 1. 命令式编程

项目采用命令式而非声明式编程：
- ✅ 明确的步骤和操作
- ✅ 易于理解和调试
- ✅ 代码像积木一样组合

### 2. 单向数据流

```
用户操作 → 事件触发 → 调用命令 → 修改Store → 组件更新
```

### 3. 关注点分离

- **Components** - 只负责UI
- **Commands** - 只负责业务逻辑
- **Utils** - 只负责计算
- **Store** - 只负责状态

### 4. 全局命令系统

所有命令挂载到 `window.cmd`，可在任意位置调用：
- 浏览器控制台
- 组件事件处理
- 其他命令函数
- 第三方脚本

### 5. 自动化

- **自动历史记录** - 鼠标操作后自动记录
- **自动更新** - Store变化自动触发组件更新
- **自动适配** - 函数自动适配多种参数格式

---

## 📖 代码阅读指南

### 如何阅读一个命令函数

以 [`createNode`](src/commands/Node.js:94) 为例：

```javascript
/**
 * 1. 先看函数注释，了解用法和参数
 */
export function createNode(opcodeOrConfig, options = {}) {
  
  /**
   * 2. 看参数处理，理解如何适配多种格式
   */
  let opcode, x, y, name, params
  if (typeof opcodeOrConfig === 'string') {
    opcode = opcodeOrConfig
    x = options.x ?? 100
    // ...
  }
  
  /**
   * 3. 看业务逻辑，理解具体操作
   */
  const nodeDef = registry?.nodes?.[opcode]
  const nodeId = generateNodeId()
  const newNode = { id: nodeId, ... }
  
  /**
   * 4. 看状态更新，理解如何修改Store
   */
  setState({ nodes: [...nodes, newNode] })
  
  /**
   * 5. 看返回值，理解函数输出
   */
  return newNode
}
```

### 如何阅读一个组件

以 [`Node.jsx`](src/components/Node.jsx:1) 为例：

```javascript
/**
 * 1. 先看组件注释，了解职责和用法
 */
const Node = ({ id, data }) => {
  
  /**
   * 2. 看数据获取，理解从哪里读取数据
   */
  const edges = useEdges()
  const color = data?.color || "rgb(137, 146, 235)"
  
  /**
   * 3. 看事件处理，理解如何响应用户操作
   */
  const onClick = (e) => {
    if (e.ctrlKey) {
      window.cmd.toggleSelectNode(id)
    } else {
      window.cmd.clearSelect()
      window.cmd.selectNode(id)
    }
  }
  
  /**
   * 4. 看渲染逻辑，理解UI结构
   */
  return (
    <div onClick={onClick}>
      {/* 输入端口 */}
      {/* 节点名称 */}
      {/* 输出端口 */}
    </div>
  )
}
```

---

## 🤝 贡献指南

### 开发流程

1. **理解需求** - 明确要实现的功能
2. **设计方案** - 确定修改哪些文件
3. **编写代码** - 遵循代码规范
4. **添加注释** - 每行代码都要有注释
5. **测试功能** - 确保功能正常工作
6. **提交代码** - 提交清晰的commit信息

### 代码审查要点

- ✅ 是否遵循命令式编程风格
- ✅ 是否有详细的注释
- ✅ 是否支持多种参数格式
- ✅ 是否正确使用Store
- ✅ 是否减少了嵌套和缩进
- ✅ 变量和函数命名是否清晰

---

## 📄 许可证

本项目代码仅供学习和参考使用。

---

## 📞 联系方式

如有问题或建议，请联系项目维护者。

---

## 🎉 总结

**炼丹蓝图**是一个设计精良、代码清晰的可视化节点编辑器项目。通过本文档，你应该能够：

✅ 理解项目的整体架构和设计理念
✅ 掌握三层架构（Components、Commands、Utils）
✅ 了解如何添加新功能和修改现有功能
✅ 遵循项目的代码规范和开发流程
✅ 快速定位和解决常见问题

**核心要点：**

1. **命令系统是核心** - 所有业务逻辑都在commands中
2. **Store是唯一数据源** - 所有状态都在store中
3. **组件只负责UI** - 不包含业务逻辑
4. **代码要易于理解** - 详细注释，减少嵌套
5. **函数要灵活** - 支持多种参数格式

**开始开发前，请务必：**

- 📖 完整阅读本文档
- 📖 阅读 [`开发目标.txt`](开发目标.txt:1)
- 🔍 浏览所有源代码文件
- 💻 运行项目并体验功能
- 🧪 在控制台测试各种命令

祝你开发顺利！🚀

---

**文档版本：** 1.0
**最后更新：** 2026-02-04
**维护者：** 炼丹蓝图开发团队
## 🔍 关键交互逻辑

### 节点交互

参考[`开发目标.txt`](开发目标.txt:51)和[`Node.jsx`](src/components/Node.jsx:1)

**单击节点：**
- 按住Ctrl：切换选中状态
- 未按Ctrl：清空选择并选中当前节点

**右键节点：**
- 选中当前节点
- 显示节点菜单和属性面板

**双击节点：**
- 打开重命名弹窗

### 端口交互

参考[`开发目标.txt`](开发目标.txt:63)和[`Node.jsx`](src/components/Node.jsx:103)

**拖拽已连接的输入端口：**
1. 检测到输入端口已有连接
2. 断开与该输出端口的连接
3. 从输出端口重新开始拖拽连线

### 画布交互

参考[`开发目标.txt`](开发目标.txt:44)和[`Blueprint.jsx`](src/components/Blueprint.jsx:1)

**空白区域点击：**
- 未按Ctrl：清空节点选择
- 按住Ctrl：保持当前选择

**拖放创建节点：**
1. 从侧边栏拖拽节点
2. 在画布上释放
3. 获取节点opcode
4. 在释放位置创建节点

### 历史记录

参考[`History.js`](src/commands/History.js:1)和[`AutoRecord.js`](src/commands/AutoRecord.js:1)

**自动记录机制：**
1. 监听鼠标松开事件
2. 对比当前状态和历史记录
3. 如果有变化则自动记录
4. 撤销/重做后状态相同则跳过记录

**手动记录：**
```javascript
window.cmd.record('操作描述')
```

---

## 🎯 常见开发任务

### 任务1：添加新的节点参数类型

1. 在 [`NodePanel.jsx`](src/components/NodePanel.jsx:1) 中添加新的输入组件
2. 在 `renderParamEditor` 函数中添加类型判断
3. 更新后端节点定义，添加新类型的参数

### 任务2：添加新的工具栏按钮

1. 在 [`ToolBar.jsx`](src/components/ToolBar.jsx:1) 中添加按钮
2. 创建对应的命令函数
3. 在按钮点击事件中调用命令

### 任务3：修改节点样式

1. 查看 [`Node.css`](src/styles/Node.css:1)
2. 使用CSS变量 `--node-color` 实现动态颜色
3. 保持样式的模块化

### 任务4：添加新的快捷键

1. 在对应组件中监听键盘事件
2. 调用相应的命令函数
3. 示例：撤销快捷键 Ctrl+Z

```javascript
useEffect(() => {
  const handleKeyDown = (e) => {
    if (e.ctrlKey && e.key === 'z') {
      window.cmd.undo()
    }
  }
  document.addEventListener('keydown', handleKeyDown)
  return () => document.removeEventListener('keydown', handleKeyDown)
}, [])
```

---


## 🚀 快速开始

### 安装依赖

```bash
# 使用yarn（推荐）
yarn install

# 或使用npm
npm install
```

### 启动开发服务器

```bash
# 使用yarn
yarn dev

# 或使用npm
npm run dev
```

访问 `http://localhost:5173` 查看应用

### 构建生产版本

```bash
# 使用yarn
yarn build

# 或使用npm
npm run build
```

---

